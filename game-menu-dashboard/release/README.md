# Game Menu Dashboard tester builds

`game-menu-dashboard-0.2.3-dev.jar` is the current remapped Fabric runtime JAR for
Minecraft 1.21.1 / Java 21. Install only this file in the target instance's `mods`
folder; do not use a development JAR or source ZIP. Verify it with
`SHA256SUMS.txt` first.

`game-menu-dashboard-0.2.1.jar` remains the prior GitHub Release artifact.
