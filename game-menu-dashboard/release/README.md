# Game Menu Dashboard 0.2.1

`game-menu-dashboard-0.2.1.jar` is the remapped Fabric runtime JAR for Minecraft
1.21.1 / Java 21. Install only this file in the target instance's `mods` folder; do
not use a development JAR or source ZIP. Verify it with `SHA256SUMS.txt` first.
